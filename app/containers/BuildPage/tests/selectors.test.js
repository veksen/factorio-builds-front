// import { fromJS } from 'immutable';
// import { selectBuildPageDomain } from '../selectors';

describe('selectBuildPageDomain', () => {
  it.skip('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
