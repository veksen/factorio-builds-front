/*
 * BuildsForm Messages
 *
 * This contains all the text for the BuildsForm component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.BuildsForm.header',
    defaultMessage: 'Add a build',
  },
});
